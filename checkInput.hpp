#ifndef CHECK_INPUT
#define CHECK_INPUT
#include <string>

//Contains the checkInput function that determines whether an input only contains an int. 
bool checkInput(std::string input);
//overloaded checkInput
//Contains the checkInput function that determines whether an input only contains an int. 
bool checkInput(std::string input, int min, int max);



#endif